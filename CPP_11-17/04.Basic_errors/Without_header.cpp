int main()
{
    int one{2}, two{3}, total{};
    total = one * two;

    // That proves we only header files to use their functionalities.
    // We can write & compile our program withour header file.
    return 0;
}